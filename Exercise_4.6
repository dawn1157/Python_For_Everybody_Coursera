#The solution for the problem 4.6:

def computepay(h, r):
    return p
h = float(input("Enter Hours:"))
r = float(input("Enter rate:"))
if h > 40:
    p = (h - 40) * r * 1.5 + 40 * r


p = computepay(h, r)
print("Pay", p)
